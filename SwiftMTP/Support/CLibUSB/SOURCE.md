# CLibUSB vendored source

- Upstream: <https://github.com/libusb/libusb>
- Tag: `v1.0.29`
- Header: `libusb/libusb.h`
- License: upstream `COPYING`, vendored as `LICENSE`

The tracked `SwiftMTP/libusb-1.0.dylib` reports version 1.0.29 through
`libusb_get_version()`. The header and license are copied unchanged from the
matching upstream tag.
